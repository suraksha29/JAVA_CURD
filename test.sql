create database test;

use test;

create table register( id int primary key auto_increment,name varchar(255),password varchar(255),emai varchar(255),sex varchar(100),country varchar(250));
 
 
 select * from  register;
 
